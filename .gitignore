# ── Credentials — NEVER commit ───────────────────────────────
.env

# ── Personal bookmark data ────────────────────────────────────
data/chroma_db/
data/edge_bookmarks.json
data/design_categorized.json
data/design_results.json
data/teaching_ai_results.json
data/*.json
data/*.html

# ── Local model files (too large for GitHub) ─────────────────
# Root-level only — do NOT clobber openmark/models/ (Foundry registry code).
/models/

# ── AI tool skill configs (machine-specific) ─────────────────
skills/
skills-lock.json
.adal/
.agents/
.augment/
.bob/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.rovodev/
.trae/
.vibe/
.windsurf/
.zencoder/

# ── Claude Code internal state ────────────────────────────────
.claude/*
# but DO commit project-specific OpenMark skills + humanizer drop-ins +
# agent-generated skills so the composer's sub-agents see the same set on a
# fresh clone:
!.claude/skills/
.claude/skills/*
!.claude/skills/openmark-*/
!.claude/skills/humanizer-*/
!.claude/skills/agent-generated-*/

# ── Newsletter drafts (personal) ─────────────────────────────
drafts/

# ── Chat history (personal) ──────────────────────────────────
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db-journal
data/openmark_chat.db

# ── MCP config (machine-specific paths) ──────────────────────
.mcp.json

# ── Session planning artifacts ────────────────────────────────
findings.md
progress.md
task_plan.md
ai_teaching_bookmarks.md
nul

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# ── Virtual environments ──────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── IDE ───────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Logs & temp ───────────────────────────────────────────────
*.log
*.tmp
*.bak

# ── HuggingFace cache ─────────────────────────────────────────
.cache/

# ── Raw data exports ─────────────────────────────────────────
raindrop-mission/
data/linkedin_saved.json
data/youtube_MASTER.json
data/CATEGORIZED.json

# ── Utility / dev scripts ─────────────────────────────────────
nsfw_*.py
nsfw_*.json
scan_nsfw.py
_*.py
_purge_now.py
app.py
space_readme.md
*.stackdump

# ── Session / handoff / research / drafts (never push to remote) ─────────
# Code only on remote. Markdown / research dossiers / generated drafts are local.
NEXT.md
SESSION_HANDOFF.md
research/
drafts/
ONBOARDING.md
*.session.md

# ── Node modules / build artifacts in the Maizzle + Astro toolchains ──────
node_modules/
openmark/publish/templates/node_modules/
openmark/publish/templates/build_local/
openmark/publish/templates/build_production/
openmark/publish/templates/data/payload.json
site/node_modules/
site/dist/
site/.astro/
.pnpm-store/
